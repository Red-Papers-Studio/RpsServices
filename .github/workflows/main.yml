---
name: CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  
  test-services:
    name: Tests Services
    uses: ./.github/workflows/test.yml
  